<form method="post" action="{{ path('app_client_delete', {'idPersonne': client.idPersonne}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ client.idPersonne) }}">
    <button class="btn">Delete</button>
</form>
