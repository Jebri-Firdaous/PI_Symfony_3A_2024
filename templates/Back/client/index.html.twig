{% extends 'Back/base.html.twig' %}
{% block stylesheets %}
<style>
   .button-link {
   display: block;
   width: fit-content;
   margin: 0 auto;
   text-align: center;
   padding: 10px 20px;
   background-color: #2f64b1;
   color: #fff; /* Couleur du texte blanc */
   text-decoration: none;
   border-radius: 5px;
   }
   .button-link:hover {
   background-color: #4a7abf; /* Couleur de fond plus claire au survol */
   color: #fff; /* Couleur du texte blanc */
   /* La couleur du texte reste toujours blanc */
   }
</style>
{% endblock %}
{% block main %}
<div class="row" style="margin-left: 2px; margin-right: 2px;">
		<div class="card flex-fill">
			<div class="card-header">
				<h2 style="font-weight: bold; color: #2f64b1; text-align: center;">Liste des Clients</h2>
			</div>
			<table class="table table-hover my-0" >
				<thead>
					<tr>
					<th>Nom</th>
					<th class="d-none d-xl-table-cell">Prénom</th>
					<th class="d-none d-xl-table-cell">Genre</th>
					<th class="d-none d-xl-table-cell">Âge</th>
					<th>Numéro de Téléphone</th>
					<th class="d-none d-md-table-cell">Adresse email</th>
					<th class="d-none d-md-table-cell">Image</th>
					<th class="d-none d-md-table-cell">Actions</th>
					</tr>
				</thead>
				<tbody>
					{% for client in list %}
					<tr>
					<td>{{ client.personne.getNomPersonne() }}</td>
					<td class="d-none d-xl-table-cell">{{ client.personne.getPrenomPersonne() }}</td>
					<td class="d-none d-xl-table-cell">{{ client.genre }}</td>
					<td>{{ client.age }}</span></td>
					<td >{{ client.personne.getNumeroTelephone() }}</td>
					<td >{{ client.personne.getMailPersonne() }}</td>
					<td >    
						<img src="{{ asset('img/image_compte/' ~ client.personne.getImagePersonne()) }}" alt="{{ client.personne.getNomPersonne() }}" width="50" height="50">
					</td>
					<td >  
					<a href="{{ path('app_client_edit_Back', {id: client.personne.getId()}) }}" title="Modifier" class="edit">
						<span class="icon">✏️</span> 
						</a>
						<a href="{{ path('app_client_delete_Back', {id: client.personne.getId()}) }}" title="Supprimer" class="delete">
						<span class="icon">🗑️</span> 
						</a>											
					</td>
					</tr>
					{% endfor %}
				</tbody>
			</table>
		</div>
</div>
<a href="{{ path('app_client_add_Back') }}" class="btn-primary" >Ajouter Client</a>
{% endblock %}