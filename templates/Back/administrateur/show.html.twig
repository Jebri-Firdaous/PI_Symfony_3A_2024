<!DOCTYPE html>

<title>Administrateur</title>

{% block body %}
    <h1>Administrateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Role</th>
                <td>{{ administrateur.role }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_administrateur_index') }}">back to list</a>

    <a href="{{ path('app_administrateur_edit', {'personne': administrateur.personne}) }}">edit</a>

    {{ include('administrateur/_delete_form.html.twig') }}
{% endblock %}
