{# reservation/_delete_form.html.twig #}

<form method="post" action="{{ path('app_reservation_delete_back', {'refReservation': reservation.refReservation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation.refReservation) }}">
    <button type="submit" class="btn btn-delete">Supprimer</button> {# Ajoutez la classe btn-delete ici #}
</form>

