{% extends 'Back/base.html.twig' %}

{% block sideBar %}
<nav id="sidebar" class="sidebar js-sidebar">
    <div class="sidebar-content js-simplebar">
        <a class="sidebar-brand" href="index.html">
  <span class="align-middle">AdminKit</span>
</a>

        <ul class="sidebar-nav">
            <li class="sidebar-header">
                Pages
            </li>

            <li class="sidebar-item">
                <a class="sidebar-link" href="index.html">
      <i class="align-middle" data-feather="sliders"></i> <span class="align-middle">Dashboard</span>
    </a>
            </li>

            <li class="sidebar-item active">
                <a class="sidebar-link" href="{{path('app_parkingB')}}">
      <i class="align-middle" data-feather="car"></i> <span class="align-middle">Parking</span>
    </a>
            </li>

            <li class="sidebar-item">
                <a class="sidebar-link" href="{{path('app_parking_stat')}}">
      <i class="align-middle" data-feather="stat"></i> <span class="align-middle">Statistic</span>
    </a>
            </li>

            <li class="sidebar-item">
                <a class="sidebar-link" href="{{path('app_parking_recy')}}">
      <i class="align-middle" data-feather="stat"></i> <span class="align-middle">Recycle</span>
    </a>
            </li>
    </div>
</nav>
{% endblock %}

{% block main %}
    {# <h1>Parking ( <a href="{{ path('app_parking_edit', {'idParking': parking.idParking}) }}"><i class="fas fa-pencil-alt"></i></a> | {{ include('Back/parking/_delete_form.html.twig') }} )</h1> #}
    <div class="content">
    <h1>
        <a href="{{ path('app_parkingB') }}" class="btn" style ="width: 50px;"><i class="fas fa-arrow-left fa-2x"></i></a>Parking (
        <span style="display: inline-block; margin-right: 10px; margin-left: 10px;">
            <a href="{{ path('app_parking_edit', {'idParking': parking.idParking}) }}"><i class="fas fa-pencil-alt"></i></a>
        </span>
        | 
        <span style="display: inline-block; margin-right: 10px; margin-left: 10px;">
            {{ include('Back/parking/_delete_form.html.twig') }}
        </span>
        )
    </h1><br>

    <table class="table">
        <tbody>
            <tr>
                <th>ID</th>
                <td>{{ parking.idParking }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ parking.nomParking }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ parking.addressParking }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ parking.latitude }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ parking.longitude }}</td>
            </tr>
            <tr>
                <th>Nombre de Place Max</th>
                <td>{{ parking.nombrePlaceMax }}</td>
            </tr>
            <tr>
                <th>Nombre de Place Occupées</th>
                <td>{{ parking.nombrePlaceOcc }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ parking.etatParking }}</td>
            </tr>
        </tbody>
    </table>

    {# <a href="{{ path('app_parkingB') }}" class="btn btn-primary" style="width: 150px;"><i class="fas fa-arrow-left"></i></a> <br> #}

    <a href="{{ path('app_place_indexB', {idP: parking.idParking}) }}" class="btn btn-primary" style="width: 150px;">Liste des places</a>

    
</div>
    
{% endblock %}
