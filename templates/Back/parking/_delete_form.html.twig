<form id="deleteForm" method="post" action="{{ path('app_parking_delete', {'idParking': parking.idParking}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ parking.idParking) }}">
    {# <button class="btn">Delete</button> #}
    <a href="#" onclick="event.preventDefault(); document.getElementById('deleteForm').submit();">
        <i class="fas fa-trash-alt"></i> <!-- Font Awesome trash icon -->
    </a>
</form>
