<!DOCTYPE html>

<title>Parking</title>

{% block body %}
    <h1>Parking</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdParking</th>
                <td>{{ parking.idParking }}</td>
            </tr>
            <tr>
                <th>NomParking</th>
                <td>{{ parking.nomParking }}</td>
            </tr>
            <tr>
                <th>AddressParking</th>
                <td>{{ parking.addressParking }}</td>
            </tr>
            <tr>
                <th>Latitude</th>
                <td>{{ parking.latitude }}</td>
            </tr>
            <tr>
                <th>Longitude</th>
                <td>{{ parking.longitude }}</td>
            </tr>
            <tr>
                <th>NombrePlaceMax</th>
                <td>{{ parking.nombrePlaceMax }}</td>
            </tr>
            <tr>
                <th>NombrePlaceOcc</th>
                <td>{{ parking.nombrePlaceOcc }}</td>
            </tr>
            <tr>
                <th>EtatParking</th>
                <td>{{ parking.etatParking }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_parking_index') }}">back to list</a>

    <a href="{{ path('app_parking_edit', {'idParking': parking.idParking}) }}">edit</a>

    {{ include('parking/_delete_form.html.twig') }}
{% endblock %}
