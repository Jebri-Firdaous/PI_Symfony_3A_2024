<!DOCTYPE html>

<title>Place</title>

{% block body %}
    <h1>Place</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>RefPlace</th>
                <td>{{ place.refPlace }}</td>
            </tr>
            <tr>
                <th>NumPlace</th>
                <td>{{ place.numPlace }}</td>
            </tr>
            <tr>
                <th>TypePlace</th>
                <td>{{ place.typePlace }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ place.etat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_place_indexF') }}">back to list</a>

    <a href="{{ path('app_place_edit', {'refPlace': place.refPlace}) }}">edit</a>

    {{ include('Front/place/_delete_form.html.twig') }}
{% endblock %}
