<form method="post" action="{{ path('app_administrateur_delete', {'personne': administrateur.personne}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ administrateur.personne) }}">
    <button class="btn">Delete</button>
</form>
