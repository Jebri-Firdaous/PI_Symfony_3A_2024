<!DOCTYPE html>
<html>
<head>
    <style>
        body {
            font-family: Arial, sans-serif;
        }

        .date-info {
            color: #888;
            font-size: 12px;
            margin-bottom: 20px;
        }

        .bottom-right-image {
            color: #888;
            font-size: 12px;
            margin-top: 20px;
        }

        .title {
            color: #be3511;
            font-size: 28px;
            text-align: center;
            font-family: 'Arial Black', sans-serif;
            margin-bottom: 20px;
        }

        .table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        th, td {
            padding: 8px;
            text-align: left;
            border-bottom: 1px solid #f2f2f2;
        }

        th {
            background-color: #f7f7f7;
            font-weight: bold;
        }

        tr:nth-child(even) {
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>

<div class="date-info">Datée le : {{ date|date('Y-m-d') }}</div>
{% block body %}
 <img src="{{ absolute_url(asset('img/logo1.png')) }}" alt="Logo E-CITY" data-aos="fade-in">
<div class="row">
    <div class="col-md-8">
        <div class="card">
            <div class="card-body">
           

                <h2 class="title">liste des billets</h2>
                
                <h3 class="subtitle"></h3>
                

                <table class="table">
                    <thead>
                        <tr>
                            <th>destination</th>
                            <th>Date</th>
                            <th>station de départ</th>
                            <th>prix</th>
                            <th>durée</th>
                           
                          
                        </tr>
                    </thead>
                    <tbody>
                        {% for billet in billets %}
                        <tr>
                            <td>{{ billet.destinationVoyage }}</td>
                            <td>{{billet.dateDepart ? billet.dateDepart|date('Y-m-d') : '' }}</td>
                            <td>{{ billet.station.nomStation }} - {{ billet.station.adressStation }} - {{ billet.station.type }}</td>
                            <td>{{ billet.prix }}DT</td>
                            <td>{{ billet.duree }}</td>
                    
                        </tr>
                        {% else %}
                            <tr>
                                <td colspan="6">Liste vide !</td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
{% endblock %}

</body>
</html>